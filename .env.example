# Public (exposed to client). Base path for API requests. Default: /api (proxied by proxy.ts when API_BACKEND_URL is set).
# NEXT_PUBLIC_API_URL=/api

# Server-only. Backend URL to which /api/* is rewritten. Leave unset to use Next.js API routes or no proxy.
# API_BACKEND_URL=https://your-backend.com
